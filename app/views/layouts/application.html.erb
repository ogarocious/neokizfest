<!DOCTYPE html>
<html>
  <head>
    <title inertia><%= content_for(:title) || "Neo Kizomba Festival" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#0f0f0f">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="canonical" href="<%= request.original_url.split('?').first %>">
    <link rel="manifest" href="/site.webmanifest">
    <meta name="description" content="<%= @meta_description || "8 Editions. 10 Years. One legacy. Neo Kizomba Festival — the official farewell and refund process." %>">
    <meta property="og:site_name" content="Neo Kizomba Festival">
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= @og_title || "Neo Kizomba Festival" %>">
    <meta property="og:description" content="<%= @meta_description || "8 Editions. 10 Years. One legacy. Neo Kizomba Festival — the official farewell and refund process." %>">
    <meta property="og:image" content="<%= request.base_url %><%= @og_image || "/images/og-default.png" %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:image" content="<%= request.base_url %><%= @og_image || "/images/og-default.png" %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= vite_react_refresh_tag %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag "application" %>
    <%= inertia_ssr_head %>
    <script defer data-domain="neokizfest.com" src="https://plausible.io/js/script.js"></script>
  </head>
  <body>
    <%= yield %>
  </body>
</html>